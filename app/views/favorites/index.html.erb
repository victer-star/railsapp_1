<div id="fh5co-portfolio">
  <div class="container">
    <div class="row">
      <h2 class="section-top">
        <span class="fas fa-heart" aria-hidden="true"></span> 
        お気に入り(<%= @favorites.count %>)
      </h2>
    </div>
    <% if @favorites.present? %>
      <ol class="favorites">
        <%= render @favorites %>
      </ol>
    <% end %>
  </div>
</div>
